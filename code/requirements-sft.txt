# Production CUDA/Linux SFT and the two reviewed catalog backends.
# Install flash-attn with the build procedure appropriate to the pinned CUDA
# toolchain; this file declares the dependency but cannot choose that toolchain.
torch>=2.4,<3
torchvision>=0.19,<1
transformers==4.57.3
accelerate>=1.1,<2
deepspeed>=0.15,<1; platform_system == "Linux"
peft==0.18.0
flash-attn>=2.7,<3; platform_system == "Linux"
qwen-vl-utils>=0.0.14,<1
PyYAML>=6,<7
decord>=0.6,<1; platform_system == "Linux"
av==12.3.0; platform_system == "Linux"
opencv-python-headless>=4.10,<5
pytorchvideo>=0.1.5,<0.2
einops>=0.8,<1
safetensors>=0.4,<1
